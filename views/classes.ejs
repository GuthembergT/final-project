<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <header>
        <nav>
          <li><a href="/:id">Profile</a></li>
          <li><a href="/members">Members</a></li>
          <li><a href="/memberships">Memberships</a></li>
          <li><a href="/locations">Locations</a></li>
          <li><a href="/classes">Classes</a></li>
          <li><a href="/calendar">Calendar</a></li>
        </nav>
    </header>

    <button> <a href="/newClass">New Class</a></button>
    
    <table id="classes">
        <th>
            <tr>Name</tr>
            <tr>Location</tr>
            <tr>Schedule</tr>
        </th>
        <% for(i = 0; i < classes.length; i++) { %>
            <tr>
                <td><a href="/class/<%= classes[i]._id%>"><%= classes[i].name %></a></td>
                <td><%= classes[i].location %></td>
                <td><% for(j = 0; j < classes[i].schedule.length; j++) { %>
                        <span><%= classes[i].schedule[j].day %>, <%= classes[i].schedule[j].start %></span><br>
                    <% } %>
                </td>
            </tr>
        <% } %>
    </table>
</body>
</html>